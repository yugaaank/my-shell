# Acrylic-behind for my-shell (Win11 taskbar match). Source from hyprland.conf:
#   source = ~/Projects/fun/my-shell/hypr/blur.conf
# Requires decoration:blur enabled. Blur applies behind surfaces only, so
# icons/text stay crisp while the translucent fills go frosted.
layerrule = blur, quickshell
